{
  "id": "openclaw-mydazy-mcp",
  "name": "MyDazy MCP",
  "description": "连接 MyDazy 设备到 OpenClaw Agent — 语音下达任务，完成后自动播报",
  "version": "0.6.4",
  "uiHints": {
    "mcpServerUrl": {
      "label": "MCP 地址",
      "help": "登录 mydazy 小程序 → 设备页面 → 复制 MCP 地址",
      "placeholder": "wss://api.xiaozhi.me/mcp/?token=...",
      "sensitive": true,
      "required": true
    },
    "webhookUrl": {
      "label": "Webhook 地址",
      "help": "登录 mydazy 小程序 → Bot 页面 → 复制 Webhook 地址",
      "placeholder": "https://...",
      "sensitive": true,
      "required": true
    },
    "triggerWord": {
      "label": "播报触发词",
      "help": "任务完成后推送到设备的触发词，最多 10 个字",
      "placeholder": "小龙虾有结果了"
    },
    "defaultAgent": {
      "label": "默认 Agent",
      "help": "执行任务的 OpenClaw Agent ID",
      "placeholder": "main"
    },
    "taskTimeoutMs": {
      "label": "任务超时 (毫秒)",
      "help": "任务执行超时时间，默认 120000 (2 分钟)",
      "advanced": true
    },
    "maxQueueSize": {
      "label": "最大队列数",
      "help": "最大排队结果数，默认 50",
      "advanced": true
    },
    "reconnectDelayMs": {
      "label": "重连延迟 (毫秒)",
      "help": "WebSocket 断开后重连等待时间，默认 5000",
      "advanced": true
    }
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["mcpServerUrl", "webhookUrl"],
    "properties": {
      "mcpServerUrl": {
        "type": "string",
        "description": "MCP WebSocket 地址 (从 mydazy 小程序获取)"
      },
      "webhookUrl": {
        "type": "string",
        "description": "Webhook 推送地址 (从 mydazy 小程序获取)"
      },
      "triggerWord": {
        "type": "string",
        "maxLength": 10,
        "default": "小龙虾有结果了",
        "description": "播报触发词"
      },
      "defaultAgent": {
        "type": "string",
        "default": "main",
        "description": "默认 Agent ID"
      },
      "maxQueueSize": {
        "type": "integer",
        "minimum": 1,
        "maximum": 200,
        "default": 50
      },
      "taskTimeoutMs": {
        "type": "integer",
        "minimum": 5000,
        "default": 120000
      },
      "reconnectDelayMs": {
        "type": "integer",
        "minimum": 500,
        "default": 5000
      }
    }
  }
}
